# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="?pXoQCTY?Wq5NUufmjsmY_PUQmX1jj8vg3`IT[iA8FEE?grocq^asYuxR`7gXoj["

# The application languages
# ~~~~~
application.langs="en,nl,no,nb"

# Mail support - not the framework one, there isn't one yet
mail.smtp.type=mock

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory.

# Root logger:
logger.root=INFO

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG
logger.CultureHub=TRACE

# CultureHub
# ~~~~~~~~~~

# Name of this node
# You MUST set a UNIQUE name when making a new deployment
cultureHub.nodeName=culturehub

# Name of the coordinating organization for this hub
cultureHub.orgId=delving

# Name of the mongo database
cultureHub.db.name=culturehub

# Enabled record-definitions for the hub
cultureHub.recordDefinitions="icn, lido"
#, tib, ese, abc, abm"

# Name of the fileStore database (for file uploads and images)
fileStore.db.name=fileStore

# Default theme for this hub
themes.defaultTheme=default

# Modules to be enabled
cultureHub.additionalModules=itin

# CultureCommons
# ~~~~~~~~~~~~~
#cultureCommons.host="http://localhost:9001"
cultureCommons.apiToken=TEST

# Demo
# ~~~~

# FastCGI IIPSRV module URL
iipsrv.url="http://localhost:8983/fastcgi/fcgi-bin/iipsrv.fcgi"


###############################################
#                                             #
#       Services OAI-PMH  Repository settings #
#                                             #
###############################################

# The name of the repository that is given in the ?verb=Identify response
services.pmh.repositoryName = Delving MetaRepo

# The administrator email that is given in the ?verb=Identify response
services.pmh.adminEmail = "somebody@delving.eu"

# The earliest Datestamp of the repository and the one that is given in the ?verb=Identify response
services.pmh.earliestDateStamp = "1990-02-01T12:00:00Z"

# The name of the OAI-PMH repository that is given in the ?verb=Identify response
services.pmh.repositoryIdentifier = meta-repo.delving.eu

# The sample identifier that is given in the ?verb=Identify response. It is constructed of the CollectionID + the MongoDB id
services.pmh.sampleIdentifier = "VerzetsMuseum:icn:250:1"

# The number of records returned by the OAI-PMH repository per ?verb=List* request
services.pmh.responseListSize = 100

# The time to live for each Harvest step before being cleaned up. So time (now) + this value is the experationDate
services.pmh.harvestStepCleanupDelay = 15000

# The name of the OAI-PMH  reposistory database in mongodb. This db is created when the application starts up the first time.
services.mongo.dbName = MetaRepo

###############################################
#                                             #
#      Solr Settings                          #
#                                             #
###############################################

solr.baseUrl = "http://localhost:8983/solr/core2"
%test.solr.baseUrl = "http://localhost:8983/solr/core3"


###############################################
#                                             #
#      DoS Settings                           #
#                                             #
###############################################

dos.tilesWorkingBaseDir=/tmp/tilesWork
dos.tilesOutputBaseDir=/tmp/tiles

# These should not need to be touched. If you do, do you know what you are doing?
dos.db.fileStore.name=fileStore
dos.db.imageCache.name=dosImageCache

# Absolute path to GraphicsMagick (e.g. /usr/local/bin/gm)
dos.graphicsmagic.cmd=/usr/local/bin/gm
%test.dos.graphicsmagic.cmd=/usr/local/bin/gm

# Relative path (within the application, in a public directory) to a dummy image to display when none is found
dos.emptyImagePath=/public/images/dummy-object.png
